name: ai_pdf_trans_apk
description: AI PDF Trans mobile client — submit PDF translation tasks to desktop server.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  web_socket_channel: ^2.4.0
  file_picker: ^8.0.0
  path_provider: ^2.1.0
  mobile_scanner: ^5.0.0
  open_filex: ^4.5.0
  shared_preferences: ^2.3.0
  provider: ^6.1.0
  share_plus: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
